# 1.2 — 用户任务文档结构解析

## 目标

识别当前待填文件的类型、标题、段落、表格区域与 **填写位置**。

## 优先级

1. Word  
2. PDF  
3. Excel  

## I/O

- **入**：任务文件路径  
- **出**：结构表示 + 位置（供 1.3 语义与统筹回填 locator）

## 实现

编辑 `service.py`；稳定 `port.py`。契约字段与统筹确认后写入 `packages/contracts`。
