# 1.3 — 语义理解与任务需求建模

## 目标

将 1.2 结构转为字段语义，并生成 **TaskSpec**，供 2.2 Agent 检索与生成。

## 依赖

- 上游：1.2 结构结果  
- 标准：2.1 **Schema**（实体/属性/关系）

## I/O

- **入**：结构 dict + schema 引用  
- **出**：TaskSpec（需查询的实体类型与属性等）

## 实现

编辑 `service.py`；TaskSpec 契约与 2.1/2.2/统筹对齐后合入 contracts。
