# contracts

Shared I/O schemas for Auto Form Fill (version in `VERSION`).

- `jsonschema/` — canonical validation
- `openapi/openapi.yaml` — HTTP API
- `python/aff_contracts/` — Pydantic mirrors for FastAPI
- `fixtures/` — golden examples

See [docs/CONTRACTS.md](../../docs/CONTRACTS.md).
